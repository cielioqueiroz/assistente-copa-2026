# Copie este arquivo para .env e preencha com a sua chave gratuita do Google Gemini.
# Pegue a chave em https://aistudio.google.com/apikey (nivel gratuito, sem cartao).
# Sem a chave, o assistente roda em modo de busca local (sem IA generativa).
GEMINI_API_KEY=coloque-sua-chave-aqui
